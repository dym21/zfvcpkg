diff --git a/image/encode/strenc.c b/image/encode/strenc.c
index c2debaa..0d28bbf 100644
--- a/image/encode/strenc.c
+++ b/image/encode/strenc.c
@@ -434,7 +434,7 @@ Int StrIOEncInit(CWMImageStrCodec* pSC)
         TCHAR szPath[MAX_PATH];
         DWORD cSize, j, k;
-#endif
         char * pFilename;
+#endif
 
         pSC->ppWStream = (struct WMPStream **)malloc(pSC->cNumBitIO * sizeof(struct WMPStream *));
         if(pSC->ppWStream == NULL) return ICERR_ERROR;
@@ -445,9 +445,7 @@ Int StrIOEncInit(CWMImageStrCodec* pSC)
             if(pSC->ppTempFile == NULL) return ICERR_ERROR;
             memset(pSC->ppTempFile, 0, pSC->cNumBitIO * sizeof(TCHAR *)); 
 #else
-            pSC->ppTempFile = (char **)malloc(pSC->cNumBitIO * sizeof(char *));
-            if(pSC->ppTempFile == NULL) return ICERR_ERROR;
-            memset(pSC->ppTempFile, 0, pSC->cNumBitIO * sizeof(char *));
+            pSC->ppTempFile = NULL;
 #endif
         }
 
@@ -479,15 +476,10 @@ Int StrIOEncInit(CWMImageStrCodec* pSC)
                     pFilename[cSize] = '\0';
                 }
 
-#else //DPK needs to support ANSI 
-                pSC->ppTempFile[i] = (char *)malloc(FILENAME_MAX * sizeof(char));
-                if(pSC->ppTempFile[i] == NULL) return ICERR_ERROR;
-
-                if ((pFilename = tmpnam(NULL)) == NULL)
-                    return ICERR_ERROR;                
-                strcpy(pSC->ppTempFile[i], pFilename);
-#endif
                 if(CreateWS_File(pSC->ppWStream + i, pFilename, "w+b") != ICERR_OK) return ICERR_ERROR;                
+#else
+                if(CreateWS_AnonymousFile(pSC->ppWStream + i) != ICERR_OK) return ICERR_ERROR;
+#endif
 
             }
             else {
diff --git a/image/sys/strcodec.c b/image/sys/strcodec.c
index 99e39d7..cbcc54e 100644
--- a/image/sys/strcodec.c
+++ b/image/sys/strcodec.c
@@ -263,10 +263,11 @@ ERR WMPFree(void** ppv)
 //================================================================
 // Streaming I/O functions
 //================================================================
-ERR CreateWS_File(struct WMPStream** ppWS, const char* szFilename, const char* szMode)
+static ERR CreateWS_FileStream(struct WMPStream** ppWS, FILE* pFile)
 {
     ERR err = WMP_errSuccess;
     struct WMPStream* pWS = NULL;
+    FailIf(NULL == pFile, WMP_errFileIO);
 
     Call(WMPAlloc((void** )ppWS, sizeof(**ppWS)));
     pWS = *ppWS;
@@ -282,17 +283,44 @@ ERR CreateWS_File(struct WMPStream** ppWS, const char* szFilename, const char* sz
     pWS->SetPos = SetPosWS_File;
     pWS->GetPos = GetPosWS_File;
 
+    pWS->state.file.pFile = pFile;
+
+Cleanup:
+    return err;
+}
+
+ERR CreateWS_File(struct WMPStream** ppWS, const char* szFilename, const char* szMode)
+{
+    ERR err;
+    FILE* pFile;
 #ifdef WIN32
-    FailIf(0 != fopen_s(&pWS->state.file.pFile, szFilename, szMode), WMP_errFileIO);
+    if (0 != fopen_s(&pFile, szFilename, szMode))
+        return WMP_errFileIO;
 #else
-    pWS->state.file.pFile = fopen(szFilename, szMode);
-    FailIf(NULL == pWS->state.file.pFile, WMP_errFileIO);
+    pFile = fopen(szFilename, szMode);
 #endif
+    if (pFile == NULL)
+        return WMP_errFileIO;
 
-Cleanup:    
+    err = CreateWS_FileStream(ppWS, pFile);
+    if (err != WMP_errSuccess)
+        fclose(pFile);
     return err;
 }
 
+ERR CreateWS_AnonymousFile(struct WMPStream** ppWS)
+{
+    FILE* pFile = tmpfile();
+    ERR err;
+    if (pFile == NULL)
+        return WMP_errFileIO;
+
+    err = CreateWS_FileStream(ppWS, pFile);
+    if (err != WMP_errSuccess)
+        fclose(pFile);
+    return err;
+}
+
 ERR CloseWS_File(struct WMPStream** ppWS)
 {
     ERR err = WMP_errSuccess;
diff --git a/image/sys/windowsmediaphoto.h b/image/sys/windowsmediaphoto.h
index fbd7538..4f8cfe7 100644
--- a/image/sys/windowsmediaphoto.h
+++ b/image/sys/windowsmediaphoto.h
@@ -313,6 +313,7 @@ struct WMPStream
 };
 
 EXTERN_C ERR CreateWS_File(struct WMPStream** ppWS, const char* szFilename, const char* szMode);
+EXTERN_C ERR CreateWS_AnonymousFile(struct WMPStream** ppWS);
 EXTERN_C ERR CloseWS_File(struct WMPStream** ppWS);
 
 EXTERN_C ERR CreateWS_Memory(struct WMPStream** ppWS, void* pv, size_t cb);
