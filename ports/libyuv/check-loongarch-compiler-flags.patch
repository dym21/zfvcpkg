diff --git a/CMakeLists.txt b/CMakeLists.txt
index f761927..db992ac 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -83,6 +83,9 @@ endif()
 
 if(LOONGARCH64)
   include(CheckCXXSourceCompiles)
+  include(CheckCXXCompilerFlag)
+  check_cxx_compiler_flag("-mlsx" SUPPORTS_MLSX_FLAG)
+  check_cxx_compiler_flag("-mlasx" SUPPORTS_MLASX_FLAG)
   set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wno-narrowing")
   check_cxx_source_compiles("
     int main(int argc, char **argv) {
@@ -98,10 +101,10 @@ if(LOONGARCH64)
        );
     return 0; }" SUPPORTS_LASX)
 
-  if(SUPPORTS_LSX)
+  if(SUPPORTS_LSX AND SUPPORTS_MLSX_FLAG)
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -mlsx")
   endif()
-  if(SUPPORTS_LASX)
+  if(SUPPORTS_LASX AND SUPPORTS_MLASX_FLAG)
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -mlasx")
   endif()
 endif()
