dav1d can be imported through CMake's FindPkgConfig module:

  find_package(PkgConfig REQUIRED)
  pkg_check_modules(dav1d REQUIRED IMPORTED_TARGET dav1d)
  target_link_libraries(main PRIVATE PkgConfig::dav1d)
