diff --git a/CMakeLists.txt b/CMakeLists.txt
index 45dfa4b..910fe33 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,6 +14,18 @@ project(boost_cobalt VERSION "${BOOST_COBALT_VERSION}" LANGUAGES CXX)
 
 include(cmake/CheckRequirements.cmake)
 if (NOT BOOST_COBALT_REQUIREMENTS_MATCHED)
+    message(STATUS "Boost.Cobalt: compiler does not support C++20 coroutines; building a stub library.")
+
+    file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/boost_cobalt_stub.cpp" "")
+
+    add_library(boost_cobalt "${CMAKE_CURRENT_BINARY_DIR}/boost_cobalt_stub.cpp")
+    add_library(Boost::cobalt ALIAS boost_cobalt)
+    target_include_directories(boost_cobalt PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>)
+
+    add_library(boost_cobalt_io "${CMAKE_CURRENT_BINARY_DIR}/boost_cobalt_stub.cpp")
+    add_library(Boost::cobalt::io ALIAS boost_cobalt_io)
+    target_link_libraries(boost_cobalt_io PUBLIC boost_cobalt)
+
     return()
 endif()
 
